<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Boot autostart -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Basic permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Telephony permissions -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />

    <!-- SMS permissions -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Audio permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.CAPTURE_AUDIO_OUTPUT" />

    <!-- SIP permissions -->
    <uses-permission android:name="android.permission.USE_SIP" />
    <uses-feature android:name="android.software.sip.voip" android:required="true" />

    <!-- System-level permissions -->
    <uses-permission android:name="android.permission.BIND_INCALL_SERVICE" />
    <uses-permission android:name="android.permission.CONTROL_INCALL_EXPERIENCE" />

    <uses-feature android:name="android.hardware.telephony" android:required="true" />

    <application
        android:name=".GatewayApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Required for ROLE_DIALER on Android 10+ -->
            <intent-filter>
                <action android:name="android.intent.action.DIAL" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="tel" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.DIAL" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
          Commissioning wizard (GW-42).

          exported="false" deliberately. It writes the SIP account, and an exported activity
          that writes configuration is a tampering surface on a device that already carries one
          unauthenticated control path (AUDIT S1/S2). The handset is rooted by definition, so
          launching it from a bench still works: `su -c am start -n
          org.onetwoone.gateway/.ui.setup.SetupActivity`.

          No launchMode and no task flags: the wizard is opened over MainActivity and must stay
          over it, so that closing it - by the Close button, by system back, by anything - lands
          on a usable console rather than an empty task. That is the whole of "never gates the
          main screen".
        -->
        <activity
            android:name=".ui.setup.SetupActivity"
            android:exported="false"
            android:label="@string/setup_title"
            android:theme="@style/Theme.Gateway.Setup" />

        <!-- InCallService - handles GSM calls -->
        <service
            android:name=".GatewayInCallService"
            android:permission="android.permission.BIND_INCALL_SERVICE"
            android:exported="true">
            <!-- UI=true required for binding as default dialer -->
            <meta-data
                android:name="android.telecom.IN_CALL_SERVICE_UI"
                android:value="true" />
            <meta-data
                android:name="android.telecom.IN_CALL_SERVICE_RINGING"
                android:value="true" />
            <!-- Allow binding for self-managed calls -->
            <meta-data
                android:name="android.telecom.INCLUDE_SELF_MANAGED_CALLS"
                android:value="true" />
            <intent-filter>
                <action android:name="android.telecom.InCallService" />
            </intent-filter>
        </service>

        <!-- PJSIP SIP service (direct PCM audio callbacks) -->
        <service
            android:name=".PjsipSipService"
            android:exported="false"
            android:foregroundServiceType="microphone|phoneCall" />

        <!-- Battery limit service (controls charging via root) -->
        <service
            android:name=".BatteryLimitService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Boot receiver for autostart -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Gateway control API receiver for external management -->
        <receiver
            android:name=".GatewayControlReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="org.onetwoone.gateway.START" />
                <action android:name="org.onetwoone.gateway.STOP" />
                <action android:name="org.onetwoone.gateway.CONFIGURE" />
                <action android:name="org.onetwoone.gateway.GET_STATUS" />
                <action android:name="org.onetwoone.gateway.TEST_CALL" />
            </intent-filter>
        </receiver>

        <!-- Disable WorkManager auto-initialization (we do it manually with error handling) -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>
</manifest>
